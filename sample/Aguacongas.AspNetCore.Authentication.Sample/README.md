# DynamicAuthProviders sample

Before running the project you need to create the database.  
Run:

``` bash
dotnet ef database update --context IdentityDataContext
```

and

``` bash
dotnet ef database update --context SchemeDbContext
```