mode: ContinuousDelivery
branches:
  master:
    lebel: ci
    is-main-branch: false
  release:
    regex: release/
    lebel: ''
    is-main-branch: true
  preview:
    regex: preview/ 
    lebel: 'preview'
    is-main-branch: true
    source-branches: ['master']
ignore:
  sha: []